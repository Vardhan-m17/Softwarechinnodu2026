'use client';
import Link from 'next/link';
import { useState } from 'react';
type Item={id:string;caption:string;permalink:string|null;status:string;title:string;company:string;location:string;salary:string;description:string;responsibilities:string;requirements:string;skills:string;external_url:string};
export default function JobImportEditor({item}:{item:Item}){const[form,setForm]=useState(item);const[busy,setBusy]=useState(false);const[message,setMessage]=useState('');function set(field:keyof Item,value:string){setForm({...form,[field]:value})}async function publish(){setBusy(true);const response=await fetch('/api/admin/instagram/publish',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({id:item.id,job:{...form,description:[form.description,form.responsibilities&&`Responsibilities:\n${form.responsibilities}`,form.requirements&&`Requirements:\n${form.requirements}`].filter(Boolean).join('\n\n'),skills:form.skills.split(',').map(x=>x.trim()).filter(Boolean)}})});const result=await response.json();setMessage(response.ok?`Published successfully. Job ID: ${result.jobId}`:result.error);setBusy(false)}async function remove(){if(!confirm('Delete this pending job?'))return;setBusy(true);await fetch('/api/admin/instagram/delete',{method:'DELETE',headers:{'Content-Type':'application/json'},body:JSON.stringify({id:item.id})});window.location.href='/admin/jobs/pending'}return <section className="edit-job-layout"><main className="edit-job-main"><nav className="edit-job-tabs"><a className="active">▣ Basic Details</a><a>▣ Job Description</a><a>☷ Responsibilities</a><a>▦ Requirements</a><a>♢ Skills & Keywords</a><a>▣ Company Details</a><a>♧ Additional Info</a></nav><section className="edit-form-card"><h2>▣ 1. Basic Information</h2><div className="edit-form-grid"><label>Job Title *<input value={form.title} onChange={e=>set('title',e.target.value)}/></label><label>Company *<input value={form.company} onChange={e=>set('company',e.target.value)}/></label><label>Job Type *<select defaultValue="Full-time"><option>Full-time</option><option>Part-time</option><option>Contract</option><option>Internship</option></select></label><label>Experience Level<input placeholder="e.g. 2–5 years"/></label><label>Salary Range<input value={form.salary} onChange={e=>set('salary',e.target.value)} placeholder="Not disclosed"/></label><label>Location *<input value={form.location} onChange={e=>set('location',e.target.value)}/></label><label>Employment Type<select defaultValue="Full-time"><option>Full-time</option><option>Part-time</option><option>Contract</option></select></label><label>Submitted Date<input value={item.status==='pending'?'Pending review':''} readOnly/></label><label>Status<span className="pending-status">{form.status}</span></label></div></section><section className="edit-form-card"><h2>▣ 2. Job Description</h2><textarea value={form.description} onChange={e=>set('description',e.target.value)} /></section><section className="edit-form-card"><h2>☷ 3. Role Responsibilities</h2><textarea value={form.responsibilities} onChange={e=>set('responsibilities',e.target.value)} placeholder="Add each responsibility on a new line" /></section><section className="edit-form-card"><h2>♧ 4. Required Skills & Qualifications</h2><textarea value={form.requirements} onChange={e=>set('requirements',e.target.value)} placeholder="Add qualifications and requirements"/><label className="edit-wide-label">Skills & keywords<input value={form.skills} onChange={e=>set('skills',e.target.value)} placeholder="React, SQL, Communication"/></label></section></main><aside className="edit-job-side"><section className="edit-side-card"><h3>▣ Company Information</h3><strong>{form.company||'Company not specified'}</strong><p>{form.location||'Location not specified'}</p><p>Imported from Instagram caption.</p></section><section className="edit-side-card"><h3>⚒ Quick Actions</h3><button className="edit-publish" onClick={publish} disabled={busy}>✓ Approve & Publish</button><button className="edit-request" disabled={busy}>♢ Request Changes</button><button className="edit-reject" onClick={remove} disabled={busy}>× Reject / Delete</button><div className="edit-side-links"><Link href={item.permalink||'#'} target="_blank">◉ View</Link><Link href="#">✎ Edit</Link></div></section><section className="edit-side-card"><h3>Job Preview</h3><strong>{form.title||'Untitled job'}</strong><p>{form.company} · {form.location}</p><span className="pending-status">{form.status}</span></section></aside>{message&&<p className="admin-empty edit-message">{message}</p>}</section>}
