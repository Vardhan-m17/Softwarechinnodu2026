import Link from 'next/link';
import { notFound } from 'next/navigation';
import { Shell } from '../../../../components/Shell';
import { createClient } from '../../../../lib/supabase/server';

export default async function ApplicantDetailsPage({ params }: { params: Promise<{ applicantId: string }> }) { const { applicantId } = await params; const supabase = await createClient(); const { data: applicant } = await supabase.from('applications').select('id,applicant_name,applicant_email,applicant_phone,status,job_id,created_at').eq('id', applicantId).maybeSingle(); if (!applicant) notFound(); return <Shell admin><div className="admin-pro-page jobs-management-page"><div className="job-details-breadcrumb"><Link href="/admin/jobs">Jobs</Link><span>›</span><span>Applicant Details</span></div><header className="admin-hero"><div><span className="admin-eyebrow">APPLICANT DETAILS</span><h1>{applicant.applicant_name || 'Applicant'}</h1><p>{applicant.applicant_email || 'Email not provided'} · Applied {new Date(applicant.created_at).toLocaleDateString('en-IN')}</p></div><span className="job-active-pill">{applicant.status}</span></header><div className="job-details-grid"><section className="admin-panel-pro"><small>PERSONAL INFORMATION</small><h2>{applicant.applicant_name || 'Applicant'}</h2><p className="job-details-copy">Email: {applicant.applicant_email || 'Not provided'}<br/>Phone: {applicant.applicant_phone || 'Not provided'}<br/>Application status: {applicant.status}</p></section><aside className="admin-panel-pro"><small>ACTIONS</small><div className="job-details-actions"><Link href="mailto:${applicant.applicant_email || ''}">Message</Link><Link href="/admin/jobs">Back to Jobs</Link></div></aside></div></div></Shell>; }
